<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.nard.myapplication.Login"
    android:background="#CCFF90">

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/Rusername"
        android:hint="Username"
        android:layout_above="@+id/Rpassword"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/Rpassword" />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/Rpassword"
        android:hint="Password"
        android:layout_above="@+id/bLogin"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        android:textColor="@color/white"
        android:id="@+id/bLogin"
        android:layout_above="@+id/tvRegister"
        android:layout_alignStart="@+id/tvRegister"
        android:layout_alignEnd="@+id/tvRegister"
        android:onClick="onButtonClick"
        android:background="@color/green"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="No Account? Sign Up!"
        android:id="@+id/tvRegister"
        android:layout_marginBottom="154dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/imageView2"
        android:src="@drawable/logo"
        android:layout_above="@+id/Rusername"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
